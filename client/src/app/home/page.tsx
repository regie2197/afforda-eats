import HomePage from "../../components/homepage/homepage";
// Ensure the correct path to the module
// If the file exists, verify the relative path is correct
export default function HomePageStores() {
  return <HomePage />;
}